# InstantSite 使い方ガイド

飲食店・小売店向けの「サイト自動生成＋編集＋解析」ツールです。
**コードを触る必要はありません。** すべて画面上で操作します。

---

## 1. 全体の流れ

```
店舗一覧（トップ）
   └ ＋新規作成 → 店の情報を入力 → 生成中（1〜2分） → 編集画面 → お客さんに提案
```

営業スタイルは「**先に作ってから見せる**」前提です。
お客さんに入力させる画面はありません。あなたがダッシュボードで情報を入れて、
完成品を持って行って提案します。

---

## 2. 新規作成（クレジット -1）

`＋新規作成` を押して、わかる範囲で埋めます。

| 項目 | 必須 | メモ |
|---|---|---|
| 店名 | ◯ | |
| 業種 | ◯ | 居酒屋／カフェ／美容室 など。雰囲気の方向性が変わる |
| 住所 | ◯ | 地図とアクセス文の生成に使う |
| 電話・営業時間・定休日 | | 入れると店舗情報セクションが充実する |
| 客層・強み | | **ここが一番効く。** 「紀州備長炭」「朝引き地鶏」など具体的に |
| 雰囲気 | | 和風・落ち着いた／ナチュラル・明るい など |
| メニュー（名前＋価格） | | 3〜5個入れれば、説明文はAIが書く |

押すと AI が
1. **配色・フォント・レイアウトを店ごとに設計**（テンプレートの使い回しではありません）
2. トップ／お品書き／店舗情報 の3ページ分の文章を執筆
3. 店の写真を生成し、WebP 3サイズに変換して軽量化

まで自動でやります。所要 1〜2分。

> クレジットを消費するのは **この生成だけ** です。
> 編集・修正・写真差し替え・AIチャット編集は何回やっても無料。

---

## 3. 編集画面（クレジット消費なし）

`編集` を押すと開きます。左が実物プレビュー、右が操作パネル。

### 文字を直す
プレビュー上の**文字を直接クリック**して打ち替えるだけ。
クリックを外した時点で自動保存されます（右上に「保存中」と出ます）。

### 写真を差し替える
プレビュー上の**写真をクリック**するとファイル選択が開きます。
選ぶと自動で WebP に圧縮・3サイズ生成されるので、重い写真をそのまま入れて大丈夫です。

### AI編集タブ
日本語で指示すると、そのページの該当箇所だけ書き換えます。

- 「もっと親しみやすい文章に」
- 「全体の配色をもう少し明るく」
- 「キャッチコピーを3案から良いものに」

生成された案は**青枠でハイライト**表示されます。`適用` で確定、気に入らなければそのまま破棄。

### 配色タブ
背景色・アクセント色・フォント・角丸を手動で調整できます。

### Googleタブ
Google マップの連携。住所から地図を埋め込みます（**キーなしでも動きます**）。
評価・口コミ・営業中バッジまで取り込みたい場合のみ `GOOGLE_MAPS_API_KEY` が必要です。
店舗一覧の上部に、キーが実際に使えているかどうかが緑／黄のバッジで出ます。

### 元に戻す
編集は履歴に残ります。おかしくなったら左上の `元に戻す` で1つ前へ。

### デバイス切替
PC / スマホ表示を切り替えて確認できます。飲食店はスマホからの流入が大半なので、
提案前にスマホ表示は必ず見てください。

---

## 4. 公開サイト

`公開サイト` ボタンで実物が開きます。URL は `/s/xxxxxxxx` 形式。
お客さんにはこの URL を送って見てもらいます。

スマホでは画面下に「電話する／予約する」の固定バーが出ます。

---

## 4-2. 予約・お問い合わせフォーム

**全店舗のサイトに自動で入ります。**（既存の店舗も、これから作る店舗も）
ページ最下部に固定で表示され、削除や設定は不要です。

お客さんが入力できる項目：

- お名前（必須）
- 電話番号 / メールアドレス（**どちらか一方は必須**）
- ご希望日・ご希望時間
- 人数
- ご要望・お問い合わせ（自由記述）

ヘッダーの `ご予約` ボタン、本文中のCTAボタン、スマホ下部の固定バーは
すべてこのフォームへジャンプします。

### 届いた予約を見る
店舗カードの **受信トレイ（封筒）アイコン** から一覧が開きます。
未対応の件数が赤いバッジで出るので、そこだけ見ていれば取りこぼしません。

各件について `未対応 → 連絡済み → 予約確定 → 来店済み`（または `キャンセル`）
のステータスを切り替えられます。電話番号・メールはタップでそのまま発信/送信できます。

送信された分は**ダッシュボードの「予約/問合」件数と転換率にも自動で反映**されます。

---

## 5. アクセス解析

店舗カードのグラフアイコンから開きます。

- **閲覧（PV）／訪問者数**
- **予約・問い合わせ件数**（電話タップ、予約ボタンのクリック）
- **転換率**（訪問者のうち何％がアクションしたか）
- 日別の推移グラフ、ページ別の内訳

月1万円のサブスクを継続してもらうための材料になります。
毎月これをスクショして「今月◯人見て、◯件問い合わせが来ました」と送るのが効きます。

---

## 6. クレジット

- 初期付与：50
- 消費：新規サイト生成 1回につき 1
- 生成に失敗した場合は自動で返却されます
- 残高は画面右上に常時表示

---

## 7. よくあるつまずき

**生成が「生成中」のまま止まる**
1〜2分かかります。3分以上変わらなければ、一度リロード。

**地図が出ない**
住所の表記ゆれが原因のことが多いです。編集画面で住所を正式表記に直してください。

**写真の雰囲気が店と違う**
写真をクリックして実店舗の写真に差し替えるのが一番早いです。
提案時は**実写に差し替えたほうが確実に刺さります**。AI画像はあくまで叩き台。

**検索結果に出したい**
公開サイトごとに、タイトル・説明文・OG画像（SNS共有時のサムネ）・
構造化データ（Google が店舗情報として読む JSON-LD）を、
入力した店舗情報から**自動生成しています**。個別の設定は不要です。

なお Runable ダッシュボードの `SEO` 設定は「このデプロイ全体で1つ」の値なので、
店舗ごとの出し分けはできません。そちらはツール自体の名前・説明のままで構いません。

**文章が硬い／店の実態と違う**
AI編集タブに「もっとくだけた口調で」「◯◯という強みを前面に」と入れて回すか、
直接クリックして打ち替えてください。
