# AIサイト自動生成・管理ツール — MVP構成案

## 0. 方針：既存の InstantSite を土台に拡張する

すでに動いているもの（作り直さない）：

- AI文章生成（Claude Sonnet 4.6 + zodスキーマで構造化出力）
- AI画像生成（Gemini 3 Pro Image / S3=Tigris保存 → `/api/media/view?key=` で配信）
- 高級和モダンのレストランテンプレート（`elegant-restaurant-site.tsx`）
- 手作りZINE風テンプレート（`site.tsx`）
- Bun + Vite + React + Hono + Drizzle(SQLite) の構成、port 4200 で稼働中

今回はこの上に「入力の拡張・複数ページ化・編集機能・ダッシュボード・クレジット・アクセス解析」を足します。ゼロから作るより2〜3倍速く、生成品質もすでに検証済みのものを引き継げます。

---

## 1. 画面の流れ

```
[1] ダッシュボード  /
     ├ 店舗カード一覧（サムネ・店名・業種・PV・予約数・転換率）
     ├ クレジット残高バッジ（例：残り 47 / 50）
     └ [＋ 新規サイトを作成] → [2]

[2] 入力フォーム  /new
     └ 店舗名 / 業種 / 住所 / 電話 / 営業時間 / 定休日 /
       メニュー（名称・価格・説明を行追加式）/ 強み・こだわり /
       ターゲット層 / 雰囲気（和風・モダン・カジュアル・ナチュラル）/
       写真アップロード（任意・最大3枚）
     └ [AIで生成する（1クレジット消費）]

[3] 生成中  /generating/:siteSlug
     └ 進捗表示：文章 → 画像 → ページ組み立て（SSEまたはポーリング）

[4] エディタ  /edit/:siteSlug
     ├ 左：ページ切替（トップ / メニュー / 店舗情報）＋ セクション一覧
     ├ 中央：実物プレビュー（iframe）— クリックでその場でテキスト編集
     ├ 右：AIチャット欄「見出しをもっと明るく」→ 差分プレビュー → 適用/取消
     └ 上部：[公開サイトを見る] [保存]  ※ここでの操作はクレジット消費なし

[5] 公開サイト  /s/:slug（トップ）/s/:slug/menu /s/:slug/info
     └ スマホ対応・PVカウント・予約/問い合わせクリックを計測

[6] 店舗別レポート  /analytics/:siteSlug
     └ 日別/週別/月別のPV推移グラフ、予約・問い合わせ件数、転換率
```

MVPはログイン不要。すべての店舗が1つのダッシュボードにぶら下がる構造で作り、後からユーザーIDの列を足すだけでマルチアカウント化できるようにします。

---

## 2. データ設計（Drizzle / SQLite）

| テーブル | 役割 | 主なカラム |
|---|---|---|
| `sites` | 店舗サイト本体（既存を拡張） | 既存 + `businessCategory`(restaurant/salon/retail/other), `mood`(和風/モダン/カジュアル/ナチュラル), `closedDays`, `targetAudience`, `strengths`, `templateId`, `status`(draft/published), `mapPlaceQuery`, `updatedAt` |
| `site_pages` | ページ単位のコンテンツ（新規） | `siteId`, `kind`(home/menu/info), `blocks`(JSON), `order` |
| `credits` | クレジット台帳（新規） | `balance` と `ledger`：`siteId`, `amount`, `reason`(generate/refund/topup), `createdAt` |
| `page_views` | PV記録（新規） | `siteId`, `pageKind`, `visitorId`(cookie), `path`, `referrer`, `createdAt` |
| `conversions` | 予約・問い合わせ計測（新規） | `siteId`, `type`(reserve_click/tel_click/form_submit), `createdAt` |
| `edit_history` | 編集の取り消し用（新規・軽量） | `siteId`, `snapshot`(JSON), `createdAt` |
| `leads` | 既存の営業リード管理 | そのまま維持 |

**コンテンツをブロックJSONで持つのが今回の肝です。** 今は「タグライン」「メニュー配列」といった固定カラムですが、これを

```json
{ "type": "hero", "headline": "...", "sub": "...", "imageKey": "..." }
{ "type": "menuList", "items": [{ "name": "...", "price": "...", "desc": "..." }] }
{ "type": "hours", "weekday": "...", "closed": "..." }
```

というブロック配列にすると、①手動編集がフィールド単位でできる ②AIチャットが「該当ブロックだけ」を書き換えられる ③業種ごとに使うブロックを差し替えるだけでテンプレート展開できる、の3つが同時に解けます。

---

## 3. 各機能の実装方針

### 3-1. AI生成（クレジット消費あり）

`POST /api/sites/generate` の1回だけがクレジットを引く場所です。

1. トランザクション内で残高チェック → 1減算 → `ledger` に記録
2. Claude で全ページ分のブロックJSONを一度に生成（zodスキーマを home/menu/info の3ページ構成に拡張）
3. 雰囲気（和風/モダン等）と業種をプロンプトと画像スタイル指定の両方に流し込む
4. 画像：アップロードがあればそれを使用、なければ Gemini で生成
5. 失敗したら残高を戻す（`reason: refund`）

### 3-2. 画像の軽量化

`sharp` を導入し、アップロード時／生成直後に処理：

- WebP に変換、`quality: 78`
- 3サイズ（640 / 1280 / 1920px）を生成して S3 に保存
- 配信側は `<img srcset>` + `loading="lazy"` + `decoding="async"`
- ヒーロー画像だけ `fetchpriority="high"` で先読み

元の生成画像（1枚 1〜2MB）が 100〜250KB 程度になり、スマホでの初回表示が体感で変わります。

### 3-3. 業種テンプレートの構造

```
templates/
  elegant/   … 既存の高級和モダン（黒×クリーム×ゴールド）
  natural/   … 明るいナチュラル系（白×木目×緑、カフェ・小売・サロン向け）
  registry.ts … { id, label, 対応業種, ブロック→コンポーネントの対応表 }
```

テンプレートは「ブロックの描画方法の集合」でしかないので、新しい業種を足すときは registry に1エントリ追加するだけ。ページ側のロジックは共通です。

### 3-4. 編集機能（クレジット消費なし）

**手動編集**：プレビュー iframe 内のテキストに `contentEditable` を付け、blur で該当ブロックのフィールドを `PATCH /api/sites/:slug/blocks/:blockId` に送るだけ。画像はクリック → アップロードダイアログ → 差し替え。

**AIチャット編集**：`POST /api/sites/:slug/ai-edit` に「見出しをもっと明るい雰囲気に」を送ると、
現在のブロックJSON全体を渡して「変更したブロックのみ」を返させる（zodスキーマで差分形式を強制）→ 変更箇所をハイライトしたプレビューを出す → ユーザーが「適用」で確定。適用前に `edit_history` にスナップショットを保存するので、1クリックで戻せます。

※ AI編集にはトークン費用がかかりますが、要件どおり**クレジットは消費しません**。無限ループ防止として1サイトあたり1日◯回といったレート制限だけ内部に持たせておくのが安全です（数値は後で調整可）。

### 3-5. Googleマップ連携

MVPは **APIキー不要の埋め込み地図**（`https://www.google.com/maps?q=<住所>&output=embed`）＋「Googleマップで開く」「経路を検索」ボタンで実装します。
将来的に Places API キーを入れれば、店舗の評価・レビュー・営業状況・公式写真の取り込みまで拡張できるよう、`mapPlaceQuery` / `placeId` の列だけ先に用意しておきます。

### 3-6. アクセス解析

公開サイト側に軽量な計測を仕込みます（外部サービス不使用、Cookieはファーストパーティのみ）。

- ページ表示時に `POST /api/track/view`（`siteId`, `pageKind`, `visitorId`）
- 予約ボタン・電話リンク・フォーム送信時に `POST /api/track/event`
- 集計は SQL の `GROUP BY date()` で日別／週別／月別
- ダッシュボード一覧に「PV / 訪問者 / 予約・問い合わせ / 転換率」を表示
- 店舗別ページに推移グラフ（Recharts）

**MVPで動かす範囲**：PVカウント＋予約/問い合わせクリック数＋転換率、そして日別グラフまで。月次レポートPDF出力は後回し（データ構造は最初から月次集計できる形にします）。

### 3-7. 数百店舗になっても重くならないための設計

- 一覧APIはページネーション（20件/ページ）＋ `sites` の `updatedAt` にインデックス
- 集計は毎回の全件走査を避け、`page_views` に `(site_id, created_at)` の複合インデックス。店舗数が増えたら日次サマリーテーブルへ自動移行できる形にしておく
- 公開サイトはサーバー側で1回組み立てて返し、店舗が増えてもビルドは不要（動的レンダリング）
- 画像はすべてS3、DBには key のみ
- 一括操作（複数店舗の営業時間をまとめて変更など）を後から足せるよう、更新APIは配列も受け取れる形に

---

## 4. 技術要素まとめ

| 領域 | 使うもの | 状態 |
|---|---|---|
| ランタイム / サーバー | Bun + Hono | 導入済み |
| フロント | React + Vite + TailwindCSS + motion | 導入済み |
| DB | SQLite + Drizzle ORM | 導入済み |
| ストレージ | Tigris (S3互換) | 導入済み |
| AI文章 | Claude Sonnet 4.6（generateObject） | 導入済み |
| AI画像 | Gemini 3 Pro Image | 導入済み |
| 画像最適化 | sharp（WebP・複数サイズ） | 新規 |
| グラフ | Recharts | 新規 |
| 地図 | Google Maps 埋め込み（キー不要） | 新規 |
| SMS（既存営業フロー） | Twilio | 未接続のまま |

---

## 5. 実装の順番

1. **DB拡張**：`site_pages` / `credits` / `page_views` / `conversions` / `edit_history` を追加、`sites` に列追加
2. **入力フォーム刷新**：業種・定休日・メニュー行追加・強み・ターゲット・雰囲気に対応
3. **生成の複数ページ化**：ブロックJSONを返すようスキーマとプロンプトを作り直し、クレジット消費を組み込む
4. **公開サイトの3ページ化**：`/s/:slug`, `/menu`, `/info`。地図埋め込みと計測タグを実装
5. **画像最適化パイプライン**：sharp で WebP 3サイズ、srcset 配信
6. **エディタ**：手動インライン編集 → AIチャット編集 → 取り消し
7. **ダッシュボード**：一覧＋クレジット残高＋指標の表示
8. **解析画面**：日別グラフと転換率

だいたい 1〜4 で「入力→生成→公開」が通り、5〜8 で「編集と管理」が揃います。

---

## 6. 確認したいこと

実装に入る前に4点だけ決めさせてください（別途フォームで質問します）。

1. 既存の「電話営業→SMS→オンボーディング」フローを残すか、ダッシュボード中心の構成に置き換えるか
2. 2つ目のテンプレートの方向性（明るいナチュラル系で進めてよいか）
3. Googleマップは埋め込みだけでよいか、Places API キーを用意して情報取り込みまでやるか
4. クレジットの初期残高と、MVPで課金（Stripe）まで繋ぐか、残高表示だけにするか
